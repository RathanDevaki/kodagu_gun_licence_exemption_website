import 'package:flutter/material.dart';

class AddTaluk extends StatelessWidget {
  const AddTaluk({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
